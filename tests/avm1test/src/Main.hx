package ;
import tink.markup.Build;

/**
 * ...
 * @author back2dos
 */

class Main {
	
	static function main() {
		//TestAll.run();
		//Build.fast($foo);
		markup.SpeedTest.run(5000);
	}
	
}