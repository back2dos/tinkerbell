package ;
import tink.markup.Build;
//import markup.SpeedTest;

class Main {
	static function main() {
		//var t:Xml = Build.xml(foo);
		//trace(t.firstChild().nodeName);
		TestAll.run();
	}
}